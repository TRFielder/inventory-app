extends layout 

block content 
    h1=title

    p #[strong Are you sure you want to delete this ingredient?]

    p #[strong Description:] #{ingredient.description}
    p #[strong Unit Price:] £#{ingredient.priceGBP}
    p #[strong In stock:] #{ingredient.quantity}
    p #[strong Category:] #{ingredient.type} 

    form(method="POST" action="")
        div 
            input#id(type="hidden",name="id", required="true", value=ingredient._id)

        button(type="submit") Delete